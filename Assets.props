<Project>
  <!-- 
    Global Assets Configuration
    Import this in your project to include global themes, fonts, and images
  -->
  
  <PropertyGroup>
    <!-- Path to global Assets folder relative to solution root -->
    <GlobalAssetsPath>$(MSBuildThisFileDirectory)</GlobalAssetsPath>
  </PropertyGroup>

  <ItemGroup>
    <!-- Include global Assets in build output, keeping Assets/ prefix -->
    <Content Include="$(GlobalAssetsPath)Assets\**\*" Exclude="$(GlobalAssetsPath)Assets\README.md">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
      <!-- TargetPath ensures Assets\ prefix is preserved in both output and when extracted from single-file -->
      <TargetPath>Assets\%(RecursiveDir)%(Filename)%(Extension)</TargetPath>
      <!-- When PackAssets=false or unset (default), exclude from bundle (keep on disk) -->
      <!-- When PackAssets=true, do NOT exclude (embed in exe and extract to temp at runtime) -->
      <ExcludeFromSingleFile Condition="'$(PackAssets)' != 'true'">true</ExcludeFromSingleFile>
      <ExcludeFromSingleFile Condition="'$(PackAssets)' == 'true'">false</ExcludeFromSingleFile>
      <Link>Assets\%(RecursiveDir)%(Filename)%(Extension)</Link>
      <Visible>false</Visible> <!-- Hide from Solution Explorer to avoid clutter -->
    </Content>
  </ItemGroup>

</Project>
